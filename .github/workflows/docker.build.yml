name: "Build Core image"

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Test query
      id: runtime
      run: |
        echo $GITHUB_REF
